# eggs depth pass — results

*2026-07-17. Brought eggs-graph from 4 Agreement raters (below `quorum` 5, so every node sat
`sealed`/pending) up to 20 (past `confirm` 15), so its lifecycle verdicts seal like covid/black-holes.
Added a 16-rater Haiku swarm (4 lens-personas × 4 seeds, bloc `cheap`) — same lenses as the existing
Sonnet panel, only model + seeds differ. `workflow_assess_tiering.js`, run blind, cooperative.*

## Outcome

- **Depth:** every node now has **n=20** Agreement ratings (was 4). No abstentions — full coverage.
- **Lifecycle states:** all-`sealed` → **18 contested + 46 provisional** (contested = the antithesis
  members, via the structural trigger). Matches covid/BH's shape; nothing stays pending on quorum.
- **`contested` verdict:** 18 contested / 116 settled / 2 ghost-eligible (node+edge).
- **Verdict preserved.** The root antithesis is unchanged in direction and nearly identical in value:
  n001 "eggs raise CVD risk" **1.62**, n002 "eggs don't" **3.71**, n048 "causal question unresolved"
  **3.96** — same story as the 4-rater panel, now backed by 20 raters.

## Cheap-vs-expert (a fourth tiering data point, mundane-contested case)

The Haiku swarm's per-node means track the Sonnet panel at **r = 0.879** across all 64 nodes —
right in line with the covid (0.85) and black-holes (0.88) tiering results. The True_R-weighted
aggregate lands between the two arms (e.g. n002: Sonnet 3.38, Haiku 3.80, weighted 3.71), so the
depth pass incorporated the cheap raters without letting them swamp the established panel. Consistent
with the standing conclusion: a cheap swarm is trustworthy for **mean/verdict and depth**, and the
durable penalty is confidence-compression on the most contested items, not the direction.
